# Secrets & captures (contain live auth tokens)
docs/captures/*.har
docs/captures/*.mitm
server/config.local.json
server/cache/
.env

# Local machine-specific Claude Code settings (permission allowlist, etc.)
.claude/settings.local.json

# Third-party reference repo kept locally only — do NOT redistribute (no license).
# Attribution + link live in README.md and the reference docs.
docs/reference/ha-inkposter-main/

# APK + local RE working files: kept in-repo folder for reference/testing, never committed
# (InkPoster's copyrighted binary). Analysis writeup lives in docs/reference/APK_ANALYSIS.md.
*.apk
*.xapk
docs/reference/apk/

# Node
node_modules/
npm-debug.log*
